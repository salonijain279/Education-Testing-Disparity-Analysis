# Data

Run `python src/generate_sample_data.py` to generate a synthetic candidate-level dataset.

The generator intentionally creates variation in preparation, distance, timing, and subject performance so the pipeline has realistic analytical cases without exposing real learners.
